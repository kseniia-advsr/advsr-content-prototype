# Anthropic Platform Console (platform.claude.com) — separate billing from a Claude Team plan.
# See "Getting Anthropic API access" in ADVSR_CONTENT_ENGINE_BUILD_SPEC.md.
ANTHROPIC_API_KEY=

# Supabase project (Settings -> API). The service role key is server-only —
# it must never be exposed to the browser or committed.
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
